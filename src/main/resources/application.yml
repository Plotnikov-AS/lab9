spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: "jdbc:mysql://${MYSQL_HOST:localhost}:3306/lab9_schema"
    username: root
    password: root